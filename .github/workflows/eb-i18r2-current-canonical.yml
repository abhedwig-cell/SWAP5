name: EB-I18R2 current-canonical bottom-energy publication

on:
  push:
    branches:
      - work/eb-i18r2-current-canonical-bottom-energy-publication
  workflow_dispatch:

permissions:
  contents: read

jobs:
  qualify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install gfortran
        run: sudo apt-get update && sudo apt-get install -y gfortran
      - name: EB-I18R2 current-canonical rebound qualification
        run: bash tests/eb/run_eb_i18r2_current_canonical_gate.sh
