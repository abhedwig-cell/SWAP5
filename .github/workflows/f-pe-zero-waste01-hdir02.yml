name: F-PE-ZERO-WASTE01 H-DIR02 drainage successor

on:
  pull_request:
    paths:
      - 'src/adapter/mod_reference_richards_accepted_step_directional_service.f90'
      - 'tests/fpe/run_fpe_zero_waste01_hdir02.sh'
      - '.github/workflows/f-pe-zero-waste01-hdir02.yml'
  workflow_dispatch:

jobs:
  drainage-successor:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Qualify direct source/sink directional assembly
        run: bash tests/fpe/run_fpe_zero_waste01_hdir02.sh
