name: F-ROM-LARE BC2-B0 prescribed moving geometry

on:
  push:
    branches:
      - work/f-rom-lare-rs1
    paths:
      - 'integration/f-rom/LARE_BC2_B0_PREREGISTRATION.json'
      - 'tests/rom/run_lare_bc2_b0_prescribed_geometry.py'
      - '.github/workflows/f-rom-lare-bc2-b0.yml'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  replay:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Install numerical dependency
        run: python3 -m pip install --disable-pip-version-check numpy

      - name: Recover qualified BC2-A2 Reference geometry
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/lare-bc2-b0/a2
          gh api "repos/${GITHUB_REPOSITORY}/actions/artifacts/10587360597/zip" > /tmp/lare-bc2-b0/a2.zip
          echo "86a7922e540a64e168835c23868fea3f67fc49d820c28901db7ba62ec3daa278  /tmp/lare-bc2-b0/a2.zip" | sha256sum -c -
          unzip -q /tmp/lare-bc2-b0/a2.zip -d /tmp/lare-bc2-b0/a2
          test -f /tmp/lare-bc2-b0/a2/o0.txt
          cmp /tmp/lare-bc2-b0/a2/o0.txt /tmp/lare-bc2-b0/a2/o2.txt
          echo 'LARE_BC2_B0_REFERENCE_BINDING=PASS'

      - name: Execute frozen prescribed-geometry replay
        run: |
          set -euo pipefail
          mkdir -p LARE_BC2_B0_EVIDENCE
          python3 tests/rom/run_lare_bc2_b0_prescribed_geometry.py \
            --reference /tmp/lare-bc2-b0/a2/o0.txt \
            --prereg integration/f-rom/LARE_BC2_B0_PREREGISTRATION.json \
            --output LARE_BC2_B0_EVIDENCE/LARE_BC2_B0_RESULT.json \
            | tee LARE_BC2_B0_EVIDENCE/analyzer.txt
          python3 - <<'PY'
          import json
          r=json.load(open('LARE_BC2_B0_EVIDENCE/LARE_BC2_B0_RESULT.json'))
          assert r['decision'] in {
              'BC2_CONSERVATIVE_MOVING_GEOMETRY_QUALIFIED',
              'BC2_CONSERVATIVE_MOVING_GEOMETRY_OUTSIDE_DOMAIN',
              'BC2_CONSERVATIVE_NUMERICALLY_BLOCKED',
              'BC2_REFERENCE_OR_GEOMETRY_INSUFFICIENT',
          }
          assert r['application_acceptance_adjudicated'] is False
          assert r['production_rom_authorized'] is False
          print('LARE_BC2_B0_FIREWALL=PASS')
          PY
          sha256sum LARE_BC2_B0_EVIDENCE/* > LARE_BC2_B0_EVIDENCE/sha256.txt

      - name: Upload BC2-B0 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lare-bc2-b0
          path: LARE_BC2_B0_EVIDENCE/
          if-no-files-found: warn
