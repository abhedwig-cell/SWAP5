name: F-CI63P current-canonical postimage reconciliation

on:
  push:
    branches:
      - qualification/f-ci63p-current-canonical-postimage-reconciliation
    paths:
      - '.github/workflows/fci-canonical.yml'
      - 'integration/f-ci/F-CI63P_STATUS.json'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  postimage-reconciliation:
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    steps:
      - name: Checkout exact postimage qualification head
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Tested head identity
        run: |
          echo "FCI63P_TESTED_HEAD=$(git rev-parse HEAD)"
          echo "FCI63P_TESTED_TREE=$(git rev-parse HEAD^{tree})"
      - name: Compiler identity
        run: gfortran --version
      - name: Run F-CI63P postimage gate
        run: bash tests/fci/run_fci63p_current_canonical_postimage_reconciliation.sh
