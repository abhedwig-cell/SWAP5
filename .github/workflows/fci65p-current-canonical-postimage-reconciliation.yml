name: F-CI65P current-canonical postimage reconciliation

on:
  push:
    branches:
      - qualification/f-ci65p-current-canonical-postimage-reconciliation
    paths:
      - '.github/workflows/fci-canonical.yml'
      - '.github/workflows/fci65p-current-canonical-postimage-reconciliation.yml'
      - 'tests/fci/run_fci65p_current_canonical_postimage_reconciliation.sh'
      - 'integration/f-ci/F-CI65P_STATUS.json'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  postimage-reconciliation:
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    steps:
      - name: Checkout exact postimage qualification head
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Tested head identity
        run: |
          echo "FCI65P_TESTED_HEAD=$(git rev-parse HEAD)"
          echo "FCI65P_TESTED_TREE=$(git rev-parse HEAD^{tree})"
      - name: Compiler identity
        run: gfortran --version
      - name: Run F-CI65P postimage gate
        run: bash tests/fci/run_fci65p_current_canonical_postimage_reconciliation.sh
