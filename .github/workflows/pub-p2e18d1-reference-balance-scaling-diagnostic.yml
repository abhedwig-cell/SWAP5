name: PUB-P2E18D1 Reference balance scaling diagnostic

on:
  push:
    branches:
      - work/pub-p2e18d1-reference-balance-scaling-diagnostic
    paths:
      - 'docs/publication/P2E18D1_REFERENCE_BALANCE_SCALING_DIAGNOSTIC_PREREGISTRATION.json'
      - 'tests/publication/test_pub_p2e18d1_reference_balance_scaling_diagnostic.f90'
      - 'tests/publication/run_pub_p2e18d1_reference_balance_scaling_diagnostic.sh'
      - '.github/workflows/pub-p2e18d1-reference-balance-scaling-diagnostic.yml'
  workflow_dispatch:

jobs:
  reference-balance-scaling-diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install gfortran
        run: sudo apt-get update && sudo apt-get install -y gfortran
      - name: Execute preregistered Reference balance-scaling diagnostic
        run: bash tests/publication/run_pub_p2e18d1_reference_balance_scaling_diagnostic.sh
