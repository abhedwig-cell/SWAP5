name: F-ROSS01 Real Face Holdout

on:
  push:
    branches:
      - work/f-ross01-fast-mfp-feasibility
    paths:
      - .github/workflows/ross01-real-face-holdout.yml
      - experiments/ross/run_ross01_face_table_holdout.py
      - experiments/ross/run_ross01_face_table_characterization.py
      - integration/f-ross/F-ROSS01_REAL_FACE_HOLDOUT_CONTRACT.json
      - integration/f-ross/F-ROSS01_REAL_FACE_TABLE_V2_DESIGN.json
      - integration/f-ross/F-ROSS01_REAL_FACE_ORACLE_CONTRACT.json
  workflow_dispatch:

jobs:
  holdout:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Runtime identity
        run: |
          python3 --version
          python3 -m pip --version
      - name: Install qualification dependencies
        run: python3 -m pip install --disable-pip-version-check numpy scipy
      - name: Run frozen real face-table holdout
        shell: bash
        run: |
          set -euo pipefail
          out="ross01-real-face-holdout"
          rm -rf "$out"
          mkdir -p "$out"
          python3 experiments/ross/run_ross01_face_table_holdout.py \
            "$out/F-ROSS01_REAL_FACE_TABLE_HOLDOUT_RESULT.json" \
            | tee "$out/F-ROSS01_REAL_FACE_TABLE_HOLDOUT_STDOUT.json"
      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: f-ross01-real-face-holdout
          path: ross01-real-face-holdout/
