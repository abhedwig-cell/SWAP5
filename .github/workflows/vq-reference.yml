name: VQ reference qualification

on:
  pull_request:
    paths:
      - 'reference/swap-4.3.1/**'
      - 'tools/vq/**'
      - 'docs/verification/**'
      - '.github/workflows/vq-reference.yml'
  push:
    branches:
      - main
    paths:
      - 'reference/swap-4.3.1/**'
      - 'tools/vq/**'
      - 'docs/verification/**'
      - '.github/workflows/vq-reference.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  corrected-reference:
    name: Verify current corrected reference
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install gate dependency
        run: python -m pip install --disable-pip-version-check PyYAML==6.0.2

      - name: Check B1.10 predecessor identity
        run: python tools/vq/b1_snapshot_identity.py --pin tools/vq/cases/b1-10-reference-pin.json

      - name: Check B1.11 admission identity and ordered preimage
        run: python tools/vq/b1_11_admission_gate.py

      - name: Recheck SWAP-002 predecessor tillage-start gate
        run: |
          gfortran --version
          python reference/swap-4.3.1/patches/SWAP-002/tests/run_tillage_start_gate.py

      - name: Check SWAP-004 source-bound type-mapping gate
        run: |
          gfortran --version
          python reference/swap-4.3.1/patches/SWAP-004/tests/run_mapping_gate.py

      - name: Check VQ gate unit tests
        run: |
          python -m unittest \
            tools.vq.test_b1_snapshot_identity \
            tools.vq.test_b2_reference_gate

      - name: Check deterministic B1.11 reconstructor syntax
        run: python -m py_compile tools/vq/b1_11_reconstruct.py
