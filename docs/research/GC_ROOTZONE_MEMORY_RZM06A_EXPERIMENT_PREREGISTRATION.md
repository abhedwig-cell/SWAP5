# GC-RZM06A H1-H4 real-SWAP experiment preregistration

Preregistration authority commit: `562853f7dd1a53c8301b0d4648f474084cfd2734`  
Scientific response seen before this preregistration: **no**

This preregistration follows the qualified sequential antecedent infrastructure. It freezes the real-SWAP falsification experiment before any post-antecedent forcing-free probe response is inspected.

## Frozen H1 pair

The fixed interface head is `H* = -0.7149999706136307 m`. Each antecedent interval lasts `0.01 d`.

- WET: native top-forcing field `-1e-5`
- DRY: native top-forcing field `+1e-5`
- origin A: WET → DRY
- origin B: DRY → WET

Both trajectories were selected because they are sequentially admissible in both orders and replay exactly. The selection did not use the later probe response. Both trajectories have equal integrated top forcing by construction.

The H1 probe is forcing-free at the top, uses the same H*, lasts `1e-4 d`, and is discarded after measurement. The response is accepted whole-window `bottom_outward_exchange_native`. H1 is supported only when both probes are valid and repeatable and `|ΔE| > 1e-18` in the native exchange quantity. The threshold will not be changed after observing the result.

## Frozen H2 search

H2 retains the existing endpoint criteria:

`|ΔW_profile| <= 1e-6`

and

`|ΔM1| >= 1e-4`.

Candidates all end after the same total antecedent time of `0.02 d`. The frozen interval alphabet, in order, is W10, D10, W, D, Z with native top-forcing values `-1e-4`, `+1e-4`, `-1e-5`, `+1e-5`, and `0`. All ordered length-2 trajectories are enumerated in product order. Every interval uses H*. Failed trajectories remain evidence. Among accepted endpoints, pairs are inspected lexicographically using only profile water and M1. The first pair satisfying both criteria is selected. Probe E_c is not inspected before selection.

If no pair matches, H2 is recorded as `NO_MATCH`. The tolerances are not loosened.

## Frozen H3 tangent test

The two H1 origins are used. For each origin, central finite differences are evaluated at H* with `δH = 1e-6 m` for probe durations `1e-4 d` and `1e-3 d`. Every plus and minus trial is generated by a fresh process that deterministically replays the full antecedent trajectory from the baseline. No candidate or continuation state is reused between signs.

The derivative is

`[E(H*+δH) - E(H*-δH)] / (2 δH)`.

State dependence and window dependence are reported separately. The frozen comparison threshold is `1e-12` native exchange per metre.

## H4 provenance rule

RZM06A keeps separate:

1. accepted whole-window bottom outward exchange;
2. terminal bottom outward flux;
3. q_swap derived from the accepted whole-window exchange;
4. the H3 physical local derivative;
5. baseline F-GC44 predictor `u` and production-facing affine `hcof/rhs`.

The baseline predictor coefficients are not relabelled as antecedent-origin physical tangents. A sign difference by itself is not a production defect claim.

## Gates and nonclaims

All scientific probes must remain read-only with respect to committed state, satisfy the existing mass gate `|residual| <= 1e-12`, and reproduce exactly under fresh-process replay before a positive disposition is admitted. H1/H2 response threshold is fixed at `1e-18` native exchange.

This experiment does not modify production physics or production coupling, does not introduce a second MODFLOW state variable, does not admit a production sign correction, and does not address RZM06B/H5 management forcing.
