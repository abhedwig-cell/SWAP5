# GC-RZM06E04 result

**Status:** qualified research evidence, support at frozen aggregate resolution  
**Date:** 2026-09-22  
**Production changes:** none  
**Workflow:** run 35747169833, job 106811670658

Both immutable E03E origins were reconstructed at canonical time and passed the preregistered state gates before any response was evaluated:

- `ΔW_profile = 0 cm`;
- `|ΔW_root30| = 4.3428876926654425e-05 cm`;
- `|ΔH16| = 0.0738671093353922 cm`.

Both strict fixed-`H_c` probes were admitted with complete mass accounting and zero mass residual. O0/O2 output was byte-identical and reversing A/B execution order changed no same-origin result.

The whole-window bottom exchange was:

- A: `0.003230778045001159 cm`;
- B: `0.0031719395246483373 cm`;
- `|ΔE| = 5.883852035282189e-05 cm`.

This is far above the unchanged `1e-18 cm` response threshold.

The admitted conclusion is deliberately resolution-bounded. At the frozen aggregate resolution `|ΔW_profile| <= 1e-4 cm` and `|ΔW_root30| <= 1e-4 cm`, identical `H_c` plus those aggregate storage coordinates do not determine a unique next whole-window interface exchange for this selected C01 pair and window.

Because `W_root30` is tolerance-matched rather than mathematically identical, E04 does not prove that exact-valued `H_c + W_profile + W_root30` are globally insufficient. It also does not establish `H16` as the unique or minimal omitted state variable.

The next step is therefore a response-blind nested-resolution state census over the same immutable state library. A tighter pair must be selected and persisted before any stronger response experiment is opened.
