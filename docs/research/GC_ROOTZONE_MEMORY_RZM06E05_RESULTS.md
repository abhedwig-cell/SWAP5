# GC-RZM06E05 result

**Status:** qualified response-blind state evidence  
**Date:** 2026-09-22  
**Production changes:** none  
**Preregistration:** `21bbfdf16edbe842647b345223454bd3507a1a03`  
**Workflow:** run 35747716569, job 106813500493

E05 tightened the aggregate-storage matching resolution over the same immutable 65-state library. No response fields were available to the census.

The number of H16-qualified pairs falls as follows:

- `1e-4 cm`: 14 pairs;
- `1e-5 cm`: 3 pairs;
- `1e-6 cm`: 1 pair;
- `1e-7 cm` and tighter: 0 pairs;
- exact reconstructed equality of both storage coordinates: 0 pairs.

The selected immutable `1e-6 cm` pair is D04/B/HOLD/control 1/duration 5 versus D04/B/IDENTITY/control 0/duration 0. It has:

- `ΔW_profile = 0 cm`;
- `|ΔW_root30| = 5.002116250096833e-07 cm`;
- `|ΔH16| = 0.01317294388797663 cm`;
- `|Δtheta16| = 3.587861510945256e-05`.

Thus the current library retains a material interface-adjacent state difference while matching both aggregate storage variables two orders of magnitude more tightly than the original E04 tolerance. The same library has a state-space resolution ceiling between `1e-6` and `1e-7 cm` for the frozen H16 gate.

E05 is state evidence only. A separate fixed-`H_c` probe is required before any response conclusion can be made at the tighter resolution.
