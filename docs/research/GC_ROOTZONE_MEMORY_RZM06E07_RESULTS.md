# GC-RZM06E07 result

**Status:** qualified state-only research evidence, pair selected at joint `1e-9 cm` resolution  
**Date:** 2026-09-22  
**Production changes:** none  
**Workflow:** run 35748744856, job 106817000793

E07 introduced a prospective deep-state construction axis using equal matrix source and sink below the upper 30 cm. Fifteen opposite-direction twins completed strict execution. O0/O2 output was byte-identical and all failed families remained fail-closed.

The selected pair is the one-step `fraction=0.0625*K0` twin:

- UPSHIFT: source node 6, sink node 16;
- DOWNSHIFT: source node 16, sink node 6.

The selected state differences are:

- `ΔW_profile = 0 cm`;
- `|ΔW_root30| = 5.560298887985482e-10 cm`;
- `|ΔH16| = 0.01425320747482317 cm`;
- cumulative prescribed internal transfer per family `= 1.9619991377092038e-4 cm`.

The pair passes the joint `1e-9 cm` storage rung. No pair passes `1e-10 cm` or the exact-equality rung while retaining the frozen `|ΔH16| >= 0.01 cm` gate.

E07 is state-only evidence. The use of qssdi and qdra is confined to prospective research state construction. Those terms are not part of the later fixed-`H_c` response forcing and are not admitted as a production coupling design.

GC-RZM06E08 is the authorized next step: reconstruct this immutable pair at common canonical time and apply the unchanged forcing-free fixed-`H_c` response probe with both storage gates set to `1e-9 cm`.
