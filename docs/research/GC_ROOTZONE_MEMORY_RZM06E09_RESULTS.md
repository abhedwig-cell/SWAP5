# GC-RZM06E09 result

**Status:** qualified state-only research evidence, exact aggregate pair selected  
**Date:** 2026-09-22  
**Production changes:** none  
**Workflow:** run 35749840536, job 106820892249

E09 confined the prospective net-zero internal redistribution to the two lowest compartments, nodes 15 and 16. Ten opposite-direction twins completed strict execution. Two LOCAL_DOWN families at rate index 2 failed closed at the first interval; all failed states remained transaction-safe. The complete O0/O2 observable output was byte-identical.

The response-blind selector found eight twins satisfying the exact numeric aggregate-equality rung while retaining `|ΔH16| >= 0.01 cm`. The selected pair is the largest-H16-separation exact twin:

- LOCAL_UP, rate fraction `0.5*K0`, 2 intervals;
- LOCAL_DOWN, rate fraction `0.5*K0`, 2 intervals.

Under the deterministic E09 binary64 aggregate reconstruction:

- `ΔW_profile = 0 cm`;
- `ΔW_root30 = 0 cm`;
- `|ΔH16| = 0.22296695246544473 cm`;
- `|Δtheta16| = 0.0006069204296688668`.

This removes the residual aggregate-storage mismatch that bounded E04, E06 and E08. E09 itself is still state-only evidence. It does not inspect or infer interface response.

The exactness claim is deliberately numerical: the two aggregate coordinates compare equal under the frozen binary64 reconstruction used by the selector. It is not a mathematical continuum identity statement.

GC-RZM06E10 is the authorized causality test: common-time reconstruct this immutable pair and apply the unchanged forcing-free fixed-`H_c` response probe. The E09 qssdi/qdra terms are provenance only and are absent from the response forcing.
