# F-ROM0 final close-gate reconciliation after ROM-0V1

## Current decision

**PROCEED_TO_ROM1A under the qualified ROM research Reference authority.**

This decision does not rewrite the historical fixed-control ROM-0 closeout. The earlier
`NO_GO_REFERENCE_AUTHORITY` remains valid evidence for the original fixed
1e-12 cm/day total-column control and for the first prescribed-head R3 proposition.

The original closeout explicitly allowed reopening only through a new independently
qualified Reference numerical-policy authority. The post-closeout R3Q1/R3Q2 and,
more strongly, R3D4 satisfy that route. ROM-0V1 then completed the last required
vertical-resolution Reference-floor measurement.

The strongest non-conflicting authority therefore governs the current ROM-0 status.

## Historical evidence retained

The following results remain immutable:

- original R3: B01 prescribed-head rise/fall fail under the frozen fixed total-rate control;
- R3R1/R3D3: always-on representation-bounded policies fail the stronger exact-overlap
  neutrality proposition;
- the 17:10 fixed-control `F-ROM0_CLOSEOUT.json` remains a historical no-go and is not edited.

They are superseded only for **current research continuation** by later separately
preregistered and qualified successor authority.

## Successor Reference authority

### R3D1 / R3D2

Both B01 fixed-control failures were classified as `RETRY_TOTAL_ONLY`:

- zero local-balance flags;
- zero pressure-head flags;
- integrated total residuals only 0.0955 and 0.14152 of the independently derived
  prospective representation bound.

No failed residual entered the bound formula.

### R3D4

R3D4 qualified the strict-first fail-closed successor policy:

1. every interval first executes the original R3 control;
2. a successful original attempt is committed unchanged and fallback is forbidden;
3. a failed attempt must leave committed lineage, revision, time and state unchanged;
4. fallback is allowed only after independent `RETRY_TOTAL_ONLY` classification and
   only when the integrated residual lies inside the pre-solve representation bound;
5. the same interval is reattempted once on a fresh backend with only the total-column
   criterion changed to the preregistered representation-derived value;
6. local balance, head criteria, dt, forcing, iteration/backtracking limits and the
   independent 1e-12 cm transaction mass gate remain unchanged.

Qualification evidence:

- 51/51 original accepted overlap steps bit-identical;
- B01: exactly two fallbacks, one in each prescribed-head direction;
- B14: zero fallbacks;
- 4/4 prescribed-head trajectories complete;
- directional lower-storage and bottom-exchange ordering passes for B01 and B14;
- maximum absolute committed transaction mass residual:
  8.673617379884035e-19 cm;
- no production/reference source mutation.

R3Q1/R3Q2 remain supporting evidence. R3Q2 independently proves exact transfer of
the qualified prescribed-head policy into the F-KT Reference-floor
sample/candidate/commit ownership path. R3D4 remains governing because it preserves
all originally accepted overlap states bit-for-bit.

## ROM-0V1 vertical Reference floor

ROM-0V1 executed the exact vertical pairs frozen in the original ROM-0 preregistration:

- B01:E1_NOMINAL_FLUX, 16x10 cm versus 32x5 cm;
- B14:E2_DRYING_FLUX, 16x10 cm versus 32x5 cm;
- both at dt = 0.0016 day and the same 160 cm physical depth;
- 32 common endpoints over 0.0512 day;
- original strict fixed-flux Reference sample controls;
- no representation-policy import and no post-result threshold construction.

Both cases complete on both geometries, all cross-grid metrics are finite, O0/O2 and
repeat identity pass, and every step satisfies the independent 1e-12 cm mass gate.

The measurement is deliberately a floor measurement, not an accuracy acceptance
threshold.

## Final Q0.1-Q0.6 adjudication

### Q0.1 Ownership — PASS

Accepted research states are produced only through the governed F-KT
sample/candidate/commit path. Failed first attempts cannot mutate committed authority
or be promoted. R3Q2 proves policy-to-F-KT transfer; R3D4 proves failed-first-attempt
immutability and strict-first overlap preservation.

### Q0.2 Reproducibility — PASS

- TA4 qualifies exact restart/replay of the retained 0.0008-day Reference candidate;
- D4 is deterministic across its frozen build/run qualification surface;
- V1 is O0/O2 and repeat bit-identical on both geometries.

The original gate requires identical experiment definitions to reproduce accepted
observations within the declared deterministic boundary. That requirement is met.

### Q0.3 Conservation — PASS

Every retained accepted trajectory is protected by the independent 1e-12 cm
transaction mass gate.

- D4 maximum committed residual: 8.673617379884035e-19 cm;
- V1 maxima remain below 3.64e-14 cm.

Failed attempts remain diagnostic evidence and are never silently retained.

### Q0.4 Bidirectional reachability — PASS

Under the separately qualified strict-first research Reference authority, both B01
and B14 complete the preregistered prescribed-head rise and fall trajectories and
satisfy the directional lower-storage and bottom-exchange ordering.

The original fixed-control R3 no-go remains recorded and is not reclassified.

### Q0.5 Reference floor — PASS / MEASURED

The complete original required matrix is now available:

- temporal: 0.0016 versus 0.0008 day on the preregistered B01/B14 fixed-flux cases;
- vertical: 16x10 cm versus 32x5 cm for B01:E1 and B14:E2.

Differences are retained per output and horizon. No universal scalar or post-result
ROM threshold is introduced.

### Q0.6 Non-mutation — PASS WITH RESEARCH-REFERENCE SCOPE

No production Richards equations, production solver selection, canonical transaction
ownership or production application temporal policy is changed.

TA5 only exposes the already supported mode-5 route to the governed research sample
seam. R3D1-D4 and V1 are research/test/evidence authority. The D4 and V1 executed
source trees remain unchanged in `src/` and `reference/` at this closeout.

No production Reference fallback or application admission is implied.

## Close decision

All six original close gates are now satisfied by the strongest current
non-conflicting authority.

`ROM0_DECISION = PROCEED_TO_ROM1A`

`ROM1A_AUTHORIZED = true`

The authorization is limited to ROM-1A state-identification preparation and reachable
trajectory-library design under the qualified ROM research Reference authority.
It does not authorize a production reduced solver, ROM closure, production Reference
fallback semantics or production application admission.

## Next bounded step

ROM-1A must begin with a new preregistration of the trajectory-library and data-split
design required by the original ROM-0 authority:

- discovery histories;
- held-out histories;
- discovery future probes;
- held-out future probes;
- material-transfer challenge;
- output/observation schema and Reference-floor interpretation rules.

No reduced coordinate, POD basis, memory variable or closure model may be selected
before that preregistration.
