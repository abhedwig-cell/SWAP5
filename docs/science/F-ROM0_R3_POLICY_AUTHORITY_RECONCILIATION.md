# F-ROM0 parallel Reference-policy authority reconciliation

The live branch contains two independently preregistered policy propositions that reached different decisions.

R3R1 required bit-identical endpoints wherever the original fixed-control trajectory converged. The always-on representation-bounded policy failed that proposition: only 8 of 51 overlap endpoints remained bit-identical. That NO-GO remains valid and is not rewritten.

R3Q1 was preregistered independently before the R3R1 result existed. It used the pre-existing PUB-P2E21/P2E18 numerical-reference budgets rather than bit identity. This is consistent with the original ROM-0 authority, whose Reference-floor purpose is to measure controlled numerical differences per output and horizon; the original authority never required distinct numerical policies to be bit-identical. Q1 passed its frozen budget-neutrality, mass, route and directional gates. R3Q2 then proved exact transfer of that qualified policy into the F-KT Reference-floor sample/candidate/commit ownership path.

Therefore the propositions are not merged into one verdict:

- original R3 remains NO-GO under the frozen fixed 1e-12 cm/day total-rate control;
- R3R1/R3D3 remain NO-GO for the stronger bit-identity proposition;
- R3Q1 is the qualified alternative research Reference numerical-policy authority;
- R3Q2 is the qualified F-KT trajectory authority under that policy.

The 17:10 ROM-0 negative closeout remains immutable historical evidence of the fixed-control state. Its own future-options clause allowed an independently qualified alternative Reference numerical policy. Q1/Q2 satisfy that route and therefore supersede the closeout only for the next research step; they do not rewrite the historical R3 result or change production Reference semantics.

The next authorized ROM-0 step is the originally required vertical-resolution Reference-floor diagnostic for B01:E1_NOMINAL_FLUX and B14:E2_DRYING_FLUX, comparing 16x10 cm with 32x5 cm over the same 160 cm profile.

ROM-1A remains blocked until that floor is measured and the complete Q0 gate set is reevaluated.
