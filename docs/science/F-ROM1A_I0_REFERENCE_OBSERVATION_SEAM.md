# F-ROM1A-I0 current-canonical Reference observation seam

ROM-1A may not recover accepted-state authority by calling a Reference solver attempt directly. Current canonical does not contain the earlier ROM-0 research sample seam, so trajectory generation is blocked until that seam is re-qualified on the current production codebase.

I0 is a narrow port of already qualified semantics. It adds a separate F-KT-owned sample result/candidate type and FMR delegation for one prescribed physical Reference advance. A sample is never an ordinary temporal mode and never changes the application runtime policy.

The seam permits only the pure-hydraulic base layout used by the ROM research authority, with Reference backend and bottom modes 2 or 5. Snow, temperature, roots, macropores, drainage-response state and other optional layouts remain fail-closed.

A failed sample cannot mutate committed state. A successful sample remains a candidate until an explicit F-KT commit checks lineage, revision and time provenance. The independent transaction mass gate remains mandatory.

No production fallback semantics are admitted by this workunit. ROM-1A trajectory generation remains blocked until the current-canonical qualification is green.

Requalification base for current canonical: `be2bffe2b3d1684e68f64862d530810ba2c6eb2e`.
