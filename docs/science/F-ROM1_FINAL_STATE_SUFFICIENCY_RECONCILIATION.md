# F-ROM1 final state-sufficiency reconciliation

## Scope

ROM-1 is closed here only for the preregistered **B01 within-material state-sufficiency scope**. The B14 material-transfer challenge is reported separately and is **not admitted**.

This does not authorize a production ROM, a production Reference fallback, or a material-general reduced state.

## Evidence chain

ROM-1A produced the governed B01 reachable-state library under the frozen strict-first research Reference policy: 768 accepted states, split into 512 discovery and 256 held-out states.

ROM1B1 showed that the nested storage-like coordinates Z1, Z2, Z4 and Z8 all admit discovery collisions relative to the pre-ROM1 B01 theta Reference floor. ROM1B2 then applied the prospectively frozen future probes to the frozen collision-pair manifests. Every one of the eight frozen pairs for every reduced nested coordinate produced formal storage-response ambiguity; no Z1/Z2/Z4/Z8 survivor remained.

ROM1B3 initially used a componentwise metric that was shown not to be invariant under the exactly invertible full contrast transform. ROM1B3Q1 corrected that governance error with the induced theta norm on the same frozen candidate family. Under that invariant metric, one minimally enriched discovery coordinate survived: **Z8+G8**, dimension 9, with zero discovery collisions and a positive minimum separation margin above the B01 theta floor.

ROM1B4 exposed the preregistered B01 held-out states without allowing held-out-driven enrichment. The frozen Z8+G8 coordinate retained zero held-out-inclusive collisions and therefore passed the B01 within-material state-separation gate.

## Material-transfer challenge

ROM1X1 then exposed B14 for the first time under the frozen B01-designed coordinate. Z8+G8 was not state-separating on B14: 351 cross-history collisions were found relative to the independently frozen B14 theta Reference floor. No B14-driven coordinate enrichment is allowed.

ROM1X2 tested the eight X1 collision pairs frozen before any B14 future probe. Across 192 pair/probe/horizon records, no formal storage-response ambiguity exceeded the pre-ROM1 B14 numerical response scales. Therefore:

- X1's B14 **state-separation transfer no-go remains valid**;
- the frozen X2 subset does **not** establish B14 predictive insufficiency;
- no claim of B14 predictive sufficiency follows either;
- B14 transfer remains **not admitted**.

## Final ROM-1 adjudication

The B01 discovery + held-out evidence is sufficient to freeze Z8+G8 as a B01-scoped predictive-state coordinate for the next closure stage. This is a positive ROM-1 gate only within B01.

The material-transfer challenge does not pass and is excluded from the next stage. Any stronger B14 interpretation requires a new separately preregistered transfer/closure study and may not adapt Z8+G8 using the already exposed B14 outcomes.

**Decision: PROCEED_TO_ROM2_B01_SCOPE_ONLY.**

ROM-2 may evaluate state-to-output/dynamics closure only for B01 using the frozen Z8+G8 coordinate and the existing B01 discovery/held-out firewall. B14 may not be used for fitting, enrichment, model selection or a transfer claim in that workstream.
