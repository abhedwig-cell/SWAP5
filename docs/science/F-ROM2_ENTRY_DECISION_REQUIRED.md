# ROM-2 entry decision required

The current ROM proposition is complete and closes at `MATERIAL_SPECIFIC_ONLY`.

The B01 result is positive: the frozen 9-dimensional `Z8+G8` coordinate separates discovery and held-out B01 states above the governed numerical Reference floor. That is real retained state-sufficiency evidence.

The preregistered cross-material gate is not passed. When the frozen B01-designed coordinate is exposed to B14 it has 351 cross-history state collisions. The subsequent eight frozen B14 collision pairs do not show formal storage-response ambiguity under their prospectively frozen future probes, but that limited negative probe result is neither predictive insufficiency nor predictive sufficiency and cannot convert the non-injective transfer result into a passed material-transfer gate.

Therefore ROM-2 cannot start under the current proposition.

No material-specific ROM-2 amendment or ROM-2 preregistration exists on the live branch or in the repository branch inventory at this checkpoint.

Three scientifically clean routes remain:

1. **New material-specific B01 closure proposition.** Treat the B01 result as a deliberately material-specific scientific object. The proposition must be preregistered before any closure/model fitting and must state why material-specific closure is scientifically valuable despite failed transfer. B14 remains exposed negative transfer evidence and cannot be reused as fresh independent validation.

2. **New uncontaminated transfer proposition.** Preserve `Z8+G8` and design a new independently frozen material/trajectory transfer challenge. Any enrichment from exposed B14 outcomes would require a different, still independent validation population.

3. **Stop the ROM track.** Retain `MATERIAL_SPECIFIC_ONLY` as the terminal result of the current proposition.

Until one of the first two routes receives explicit new authority, no closure identification, reduced-dynamics fitting, POD/modal fitting, memory-state fitting or production-ROM work is permitted.

This is a governance boundary rather than a technical blocker.
