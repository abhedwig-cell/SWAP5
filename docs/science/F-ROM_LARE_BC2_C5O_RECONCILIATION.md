# F-ROM-LARE BC2-C5O placement-versus-closure identifiability

C5O reconciles the fresh C5N result with the earlier closure and bottom-refinement evidence.

C5N proves that adding states upward in the frozen lower-zone ladder does not rescue BC1 CURRENT_LAYER_FACE against high-resolution B14 prescribed-head Reference controls. It does not prove that state placement cannot rescue the representation: R5 through R16 all retain the same 150-160 cm bottom layer, so the dominant boundary-adjacent state resolution remains fixed at 10 cm.

That distinction matters because C5C independently showed strong sensitivity when the bottom layer was refined from 10 cm to 5 cm and 2.5 cm. C5N also independently replicated equal-dimension placement effects (R4 versus U4 and R8 versus U8).

The earlier closure sequence remains binding. C4H identifies gradient reconstruction as the dominant internal-qi mechanism. C4L, C4M, C4N and C4O reject cubic, central, geometry-damped and single-state scalar-conditioned corrections as transferable closures. C4K provides no current evidence for an extra memory state.

Therefore C5O does not authorize another scalar closure modification and does not yet authorize a wholly new nonlinear closure implementation. The next clean discriminator is fixed-dimension redistribution of an unchanged state budget toward the bottom boundary under the unchanged CURRENT_LAYER_FACE closure.

C5P will freeze bottom-focused 5-cm and 2.5-cm variants before response, use fresh B14 dynamic-head histories, and compare them to their same-dimension 10-cm baselines against qualified high-resolution Reference controls. This tests where state should be placed before changing how state is propagated.
