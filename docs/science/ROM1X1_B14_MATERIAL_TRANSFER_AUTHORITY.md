# ROM-1X1 B14 material-transfer authority

The reduced coordinate is frozen before any B14 trajectory outcome is inspected.

Transfer changes only the material parameters from B01 to the already declared B14
challenge. The 12 history symbol sequences, time step, normalized forcing amplitudes
and strict-first research Reference policy remain unchanged.

The coordinate remains Z8+G8. No material-dependent band selection, weight or
additional state variable is allowed.

Numerical distinguishability uses the B14-specific theta Reference scale measured
before ROM-1: 1.6414144244913942e-6. This is the ROM-0V1 B14 vertical theta floor and
is larger than the B14 temporal floors from ROM-0T1Q1.

The B14 library is first a Reference qualification. The frozen strict-first policy is
applied exactly as already defined; any unclassified retry or hard-mass failure makes
the transfer library unresolved rather than authorizing a new fallback.

If the library is complete, all cross-history B14 pairs are tested with the same
coordinate-invariant induced theta norm. Any hidden-state collision rejects transfer.
B14 outcomes may not enrich or retune the coordinate.
