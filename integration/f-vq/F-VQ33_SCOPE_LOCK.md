# F-VQ33 scope lock

F-VQ33 independently qualifies only the production binding of the explicit Richards head-budget temporal certificate.

In scope:

- explicit numerical-config budget ownership;
- `C_h = B_inf / H_budget` production normalization;
- accept/reject threshold semantics;
- missing/invalid budget fail-closed behavior;
- no-history fail-closed behavior without bootstrap commit;
- bounded retry and exact rollback;
- hard mass-gate precedence;
- diagnostics and solve-cost shape;
- O0/O2 identity;
- source-locked regressions.

Out of scope:

- universal or automatic budget selection;
- dynamic tolerance control;
- groundwater/MODFLOW coupling;
- fallback solver policy;
- BALANCED/THROUGHPUT policy;
- restart bootstrap;
- file I/O;
- any claim that `B_inf` is a proven nonlinear true-error upper bound;
- F-GC02 release.
