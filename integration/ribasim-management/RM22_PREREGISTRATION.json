{
  "schema": "swap5.ribasim_management.temporal_diagnostic_aggregation_repair.preregistration.v1",
  "work_unit": "RM22",
  "date": "2026-09-23",
  "title": "Retry-complete max_temporal_indicator diagnostic repair",
  "depends_on": {
    "RM21": "DIAGNOSTIC_AGGREGATION_DEFECT_CONFIRMED"
  },
  "scope": "Diagnostic aggregation only. Preserve every transaction accept/reject decision and preserve transaction_result.temporal_indicator as the terminal solver-successful attempt value.",
  "requirements": [
    "Add a distinct transaction_result maximum that observes every finite nonnegative temporal indicator produced by a solver-successful retry attempt.",
    "Do not update the maximum on solver-failed attempts because no valid temporal certificate exists there.",
    "Canonical interval diagnostics must aggregate the new retry-complete maximum rather than the terminal transaction temporal_indicator.",
    "The frozen RM19 transaction must retain status, attempts, retries, solver_rejections, temporal_rejections, rollbacks and zero accepted substeps exactly.",
    "The repaired max_temporal_indicator for the frozen RM19 case must equal the RM21 full-window value 361493.0146731796 within floating-point tolerance.",
    "The terminal transaction temporal_indicator remains 92569.71928978531 for provenance compatibility."
  ],
  "failure_rule": "No temporal budget, retry scale, retry count, solver tolerance, forcing, state or acceptance branch may change.",
  "nonclaims": [
    "RM22 does not make the active-irrigation transaction admissible.",
    "RM22 does not establish a scientifically valid temporal budget.",
    "RM22 does not rerun the three-model triangle."
  ]
}
